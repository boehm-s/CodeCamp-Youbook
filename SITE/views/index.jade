extends layout

block head
  link(rel='stylesheet', href='/css/home.css')
  script.
    if (localStorage.titchToken === undefined)
        window.location.href = '/connection';
block body
  h1= title
  p Welcome to #{title}
block scripts
  script(src='/js/home.js')